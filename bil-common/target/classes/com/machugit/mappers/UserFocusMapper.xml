<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.machugit.mappers.UserFocusMapper">

	<!--实体映射-->
	<resultMap id="base_result_map" type="com.machugit.entity.po.UserFocus">
		<!--关注id-->
		<result column="focus_id" property="focusId"  />
		<!--用户id-->
		<result column="user_id" property="userId"  />
		<!--关注的用户id-->
		<result column="focus_user_id" property="focusUserId"  />
		<!--创建时间-->
		<result column="create_time" property="createTime"  />
	</resultMap>


	<!-- 通用查询结果列-->
	<sql id="base_column_list">
		 f.focus_id,f.user_id,f.focus_user_id,f.create_time
	</sql>

	<sql id="base_condition_filed">
			<if test="query.focusId != null">
				 and  f.focus_id = #{query.focusId}
			</if>
			<if test="query.userId != null and query.userId!=''">
				 and  f.user_id = #{query.userId}
			</if>
			<if test="query.focusUserId != null and query.focusUserId!=''">
				 and  f.focus_user_id = #{query.focusUserId}
			</if>
			<if test="query.createTime != null and query.createTime!=''">
				 <![CDATA[ and  f.create_time=str_to_date(#{query.createTime}, '%Y-%m-%d') ]]>
			</if>
	</sql>

	<!-- 通用查询条件列-->
	<sql id="query_condition">
	 <where>
			<include refid="base_condition_filed" />
			<if test="query.createTimeStart!= null and query.createTimeStart!=''">
				 <![CDATA[ and  f.create_time>=str_to_date(#{query.createTimeStart}, '%Y-%m-%d') ]]>
			</if>
			<if test="query.createTimeEnd!= null and query.createTimeEnd!=''">
				 <![CDATA[ and  f.create_time< date_sub(str_to_date(#{query.createTimeEnd},'%Y-%m-%d'),interval -1 day) ]]>
			</if>
	 </where>
	</sql>

	<!-- 查询集合-->
	<select id="selectList" resultMap="base_result_map" >
		 SELECT <include refid="base_column_list" /> FROM user_focus f <include refid="query_condition" />
		 <if test="query.orderBy!=null">
			 order by ${query.orderBy}
		 </if>
		 <if test="query.simplePage!=null">
			 limit #{query.simplePage.start},#{query.simplePage.end}
		 </if>
	</select>

	<!-- 查询数量-->
	<select id="selectCount" resultType="java.lang.Integer" >
		 SELECT count(1) FROM user_focus f <include refid="query_condition" />
	</select>

	<!-- 插入 （匹配有值的字段）-->
	<insert id="insert" parameterType="com.machugit.entity.po.UserFocus">
		 INSERT INTO user_focus
		 <trim prefix="(" suffix=")" suffixOverrides="," >
			<if test="bean.focusId != null">
				 focus_id,
			</if>
			<if test="bean.userId != null">
				 user_id,
			</if>
			<if test="bean.focusUserId != null">
				 focus_user_id,
			</if>
			<if test="bean.createTime != null">
				 create_time,
			</if>
		 </trim>
		 <trim prefix="values (" suffix=")" suffixOverrides="," >
			<if test="bean.focusId!=null">
				 #{bean.focusId},
			</if>
			<if test="bean.userId!=null">
				 #{bean.userId},
			</if>
			<if test="bean.focusUserId!=null">
				 #{bean.focusUserId},
			</if>
			<if test="bean.createTime!=null">
				 #{bean.createTime},
			</if>
		 </trim>
	</insert>

	<!-- 插入或者更新 （匹配有值的字段）-->
	<insert id="insertOrUpdate" parameterType="com.machugit.entity.po.UserFocus">
		 INSERT INTO user_focus
		 <trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="bean.focusId != null">
				 focus_id,
			</if>
			<if test="bean.userId != null">
				 user_id,
			</if>
			<if test="bean.focusUserId != null">
				 focus_user_id,
			</if>
			<if test="bean.createTime != null">
				 create_time,
			</if>
		 </trim>
		 <trim prefix="values (" suffix=")" suffixOverrides=",">
			<if test="bean.focusId!=null">
				 #{bean.focusId},
			</if>
			<if test="bean.userId!=null">
				 #{bean.userId},
			</if>
			<if test="bean.focusUserId!=null">
				 #{bean.focusUserId},
			</if>
			<if test="bean.createTime!=null">
				 #{bean.createTime},
			</if>
		 </trim>
		 on DUPLICATE key update
		 <trim prefix="" suffix="" suffixOverrides=",">
			<if test="bean.focusId!=null">
				 focus_id = VALUES(focus_id),
			</if>
			<if test="bean.userId!=null">
				 user_id = VALUES(user_id),
			</if>
			<if test="bean.focusUserId!=null">
				 focus_user_id = VALUES(focus_user_id),
			</if>
			<if test="bean.createTime!=null">
				 create_time = VALUES(create_time),
			</if>
		 </trim>
	</insert>

	<!-- 添加 （批量插入）-->
	<insert id="insertBatch" parameterType="com.machugit.entity.po.UserFocus">
		 INSERT INTO user_focus(
			 focus_id,
			 user_id,
			 focus_user_id,
			 create_time
		 )values
		 <foreach collection="list" item="item" separator=",">
			 (
				 #{item.focusId},
				 #{item.userId},
				 #{item.focusUserId},
				 #{item.createTime}
			 )
		 </foreach>
	</insert>

	<!-- 批量新增修改 （批量插入）-->
	<insert id="insertOrUpdateBatch" parameterType="com.machugit.entity.po.UserFocus">
		 INSERT INTO user_focus(
			 focus_id,
			 user_id,
			 focus_user_id,
			 create_time
		 )values
		 <foreach collection="list" item="item" separator=",">
			 (
				 #{item.focusId},
				 #{item.userId},
				 #{item.focusUserId},
				 #{item.createTime}
			 )
		 </foreach>
			on DUPLICATE key update
			focus_id = VALUES(focus_id),
			user_id = VALUES(user_id),
			focus_user_id = VALUES(focus_user_id),
			create_time = VALUES(create_time)
	</insert>

	<!--多条件修改-->
	<update id="updateByParam" parameterType="com.machugit.entity.query.UserFocusQuery">
		 UPDATE user_focus f
 		 <set>
			<if test="bean.focusId != null">
				 focus_id = #{bean.focusId},
			</if>
			<if test="bean.userId != null">
				 user_id = #{bean.userId},
			</if>
			<if test="bean.focusUserId != null">
				 focus_user_id = #{bean.focusUserId},
			</if>
			<if test="bean.createTime != null">
				 create_time = #{bean.createTime},
			</if>
 		 </set>
 		 <include refid="query_condition" />
	</update>

	<!--多条件删除-->
	<delete id="deleteByParam">
		 delete f from user_focus f
 		 <include refid="query_condition" />
	</delete>

	<!-- 根据FocusId修改-->
	<update id="updateByFocusId" parameterType="com.machugit.entity.po.UserFocus">
		 UPDATE user_focus
 		 <set>
			<if test="bean.userId != null">
				 user_id = #{bean.userId},
			</if>
			<if test="bean.focusUserId != null">
				 focus_user_id = #{bean.focusUserId},
			</if>
			<if test="bean.createTime != null">
				 create_time = #{bean.createTime},
			</if>
 		 </set>
 		 where focus_id=#{focusId}
	</update>

	<!-- 根据FocusId删除-->
	<delete id="deleteByFocusId">
		delete from user_focus where focus_id=#{focusId}
	</delete>

	<!-- 根据PrimaryKey获取对象-->
	<select id="selectByFocusId" resultMap="base_result_map" >
		select <include refid="base_column_list" /> from user_focus f where focus_id=#{focusId}
	</select>

	<!-- 根据UserId修改-->
	<update id="updateByUserId" parameterType="com.machugit.entity.po.UserFocus">
		 UPDATE user_focus
 		 <set>
			<if test="bean.focusId != null">
				 focus_id = #{bean.focusId},
			</if>
			<if test="bean.focusUserId != null">
				 focus_user_id = #{bean.focusUserId},
			</if>
			<if test="bean.createTime != null">
				 create_time = #{bean.createTime},
			</if>
 		 </set>
 		 where user_id=#{userId}
	</update>

	<!-- 根据UserId删除-->
	<delete id="deleteByUserId">
		delete from user_focus where user_id=#{userId}
	</delete>

	<!-- 根据PrimaryKey获取对象-->
	<select id="selectByUserId" resultMap="base_result_map" >
		select <include refid="base_column_list" /> from user_focus f where user_id=#{userId}
	</select>

	<!-- 根据FocusUserId修改-->
	<update id="updateByFocusUserId" parameterType="com.machugit.entity.po.UserFocus">
		 UPDATE user_focus
 		 <set>
			<if test="bean.focusId != null">
				 focus_id = #{bean.focusId},
			</if>
			<if test="bean.userId != null">
				 user_id = #{bean.userId},
			</if>
			<if test="bean.createTime != null">
				 create_time = #{bean.createTime},
			</if>
 		 </set>
 		 where focus_user_id=#{focusUserId}
	</update>

	<!-- 根据FocusUserId删除-->
	<delete id="deleteByFocusUserId">
		delete from user_focus where focus_user_id=#{focusUserId}
	</delete>

	<!-- 根据PrimaryKey获取对象-->
	<select id="selectByFocusUserId" resultMap="base_result_map" >
		select <include refid="base_column_list" /> from user_focus f where focus_user_id=#{focusUserId}
	</select>

</mapper>
